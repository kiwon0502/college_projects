#include <iostream>
#include <algorithm>
#include <vector>
#define ll long long int
using namespace std;

ll r, c;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>r>>c;
    for (ll i = 0; i < r; i++) {
        
    }

    return 0;
}